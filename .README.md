## Github tutorial for PM402
This is a codebase that runs two models in R and helps practise using git.

### Useful commands
1. git add .  (put first before adding something)
2. git commit -m "some useful message about the code change" (lets you comment)
3. git push (pushes your changes)
4. git log
5. git status
